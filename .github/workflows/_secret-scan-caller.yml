name: Secret Scan

# Dogfood the reusable secret-scan workflow on this repo.
# (Other repos copy this 8-line caller; see README.md.)

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  secret-scan:
    uses: selamy-labs/.github/.github/workflows/secret-scan.yml@main
